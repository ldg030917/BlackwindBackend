import express, { Router } from "express";
import { AppDataSource } from "../data-source";

const router = Router();

router.post("/", async (req, res) => {
  try {
    //const {}
  } catch (error) {
    
  }
})